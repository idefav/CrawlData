using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CrawlServices
{
    class Program
    {
        static void Main(string[] args)
        {
            //TaskManager taskMgr = new TaskManager();
            //taskMgr.Start();
            //Console.ReadLine();



            //string server = "Fund.Service.Memcache";
            //string description = "Memcache生成服务";

            //if (args.Length >= 2)
            //{
            //    server = args[1];
            //    InstallService iS = new InstallService();
            //    if (args[0] == "add")
            //    {
            //        if (args.Length == 3)
            //            description = args[2];
            //        var result = iS.Install(System.Reflection.Assembly.GetExecutingAssembly().Location, server, server, description);
            //    }
            //    if (args[0] == "del")
            //        iS.UnInstall(server);

            //}
            //else
            //{
            //    ServiceBase[] ServicesToRun;
            //    ServicesToRun = new ServiceBase[]
            //{
            //        new SynService()
            //};
            //    ServiceBase.Run(ServicesToRun);
            //}
        }
    }
}
